{
  "private": true,
  "scripts": {
    "start": "homebridge -R -U $PWD/config -P $PWD/homebridge-daikin-plugin",
    "reset": "rimraf config/accessories; rimraf config/persist"
  },
  "dependencies": {
    "bonjour": "^3.5.0",
    "homebridge": "^0.4.49",
    "mdns-resolver": "0.0.3"
  },
  "devDependencies": {
    "rimraf": "^2.6.3"
  }
}
